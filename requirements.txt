# Prefer `uv sync` (uses `pyproject.toml` + `uv.lock`) over `pip install -r requirements.txt`.

dash[diskcache]>=3.0
dash-bootstrap-components>=1.0
numpy>=1.26
pandas>=1.5
plotly>=5.0
scikit-learn>=1.2
scipy>=1.10

# Optional dependencies (see extras in `pyproject.toml`):
# arch>=6
# hurst>=0.0.5
# matplotlib>=3.7
# statsmodels>=0.14
# stumpy>=1.12
# tqdm>=4.66
# tsfresh>=0.20
